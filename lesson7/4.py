'''

Запросить высоту елочки - число от 3 до 20. 
Напечатать на экране елочку где ее высота равна числу строк. 
Пример елочки из 4 строк:
   *
  ***
 *****
*******
'''



'''
# Запросим высоту ёлочки
height = int(input("Введите высоту ёлочки (от 3 до 20): "))

# Проверим, что высота находится в пределах от 3 до 20
if 3 <= height <= 20:
    # Для каждой строки ёлочки
    for i in range(height):
        # Считаем количество пробелов и звездочек
        spaces = height - i - 1  # Количество пробелов
        stars = 2 * i + 1  # Количество звездочек
        print(" " * spaces + "*" * stars)
else:
    print("Высота должна быть от 3 до 20.")

'''



'''
* - елочка со снегом
'''

import random                              

height = int(input("Введите высоту ёлочки (от 3 до 20): "))     

# Проверяем корректность ввода в пределах заданных чисел
if 3 <= height <= 20:

    # Начинаем цикл по строчкам ёлочки
    for i in range(height):

        # левая часть строки (где будут пробелы или снег)
        left_width = height - i - 1
        # ширина ёлки
        tree_width = 2 * i + 1
        # правая часть строки
        right_width = left_width

        # Генерим левый снег
        left = ""
        for _ in range(left_width):
            left += random.choice([" ", ".", "*", "❄", " "])  # пробелов больше

        # Генерим саму ёлку
        tree = "*" * tree_width

        # Генерируем правый снег
        right = ""
        for _ in range(right_width):
            right += random.choice([" ", ".", "*", "❄", " "])

        # Печатаем строку
        print(left + tree + right)

else:
    print("Высота должна быть от 3 до 20.")









'''
- модуль random, чтобы случайным образом генерировать символы для снега 
(например, "*" и "❄"), который будет появляться слева и справа от ёлки
- for i in range(height) - цикл будет повторяться от 0 до height-1 (например, если высота 4, то цикл пройдет 4 раза).
В каждой итерации будет строиться одна строка ёлочки. Таким образом, для каждой строки мы рассчитываем количество 
пробелов слева, количество звездочек и количество пробелов справа.

Расчет кол-ва пробелов слева и ширины елки:
- кол-во пробелов:   "left_width = height - i - 1"  - Каждый следующий уровень ёлочки будет иметь меньше пробелов слева. Начинать с самого 
большого числа пробелов (всего height-1 пробелов на первой строке) и уменьшать на 1 для каждой последующей строки.
- кол-во звездочек для этого уровня:  "tree_width = 2 * i + 1"  - кол-во звездочек на кажд.уровне увеличивается с каждым шагом: 
на первой строке 1 звезда (2 * 0 + 1), на второй - 3 звезды (2 * 1 + 1), на третьей 5 5 звезд и т.д. Этим создаем форму треугольника для ёлочки.
- кол-во пробелов справа: "right_width = left_width "  - ёлочка симметрична, кол-во пробелов там и там будет одинаковым

- Генерация "снега" слева:
left = ""
for _ in range(left_width):
    left += random.choice([" ", ".", "*", "❄", " "])
создаем строку left, которая будет содержать символы "снега" или пустоты. 
random.choice([" ", ".", "*", "❄", " "]) выбирает случайный символ из списка  пробел или снежинки, кот случайным образом попадут в лев.часть строки
Кол-во символов, кот.будут добавляться в строку left, зависит от значения left_width, кот.вычисляется ранее.

- Генерация Ёлки 
tree = "*" * tree_width
На каждой строке ёлки печатаются только звездочки (*)
Количество звездочек равно tree_width, его мы вычисляли ранее

- Аналогично генерится снег справа

- Вывод ёлки
print(left + tree + right)
соединяем все три части (левый снег, сама ёлка и правый снег) в одну строку и выводим её на экран
'''