'''
Даны данные пользователя в строке:
    s = "имя: Дмитрий, фамилия: Иванов, возраст: 18"
    Программа должна достать из этой строки, имя, фамилию и возраст пользователя 
    присвоить их отдельным переменным и вывести на экран  через один print(), 
    но каждый элемент должен быть выведен в новой строке. 
    Программа должна работать с любой аналогичной строкой с данными др. человека.
        пример вывода:        
            - Дмитрий
            - Иванов
            - 18

'''

s = "имя: Дмитрий, фамилия: Иванов, возраст: 18"

# Делим строку на части по запятой
parts = s.split(", ")
# Создаём словарь из частей
data = {}
for part in parts:
    key, value = part.split(": ")
    data[key] = value

# Присваиваем значения переменным
name = data.get("имя", "")
surname = data.get("фамилия", "")
age = data.get("возраст", "")

# Вывод каждого элемента в новой строке
print(f"- {name}\n- {surname}\n- {age}")


'''
s = "имя: Дмитрий, фамилия: Иванов, возраст: 18"
name, surname, age = [part.split(': ')[1] for part in s.split(', ')]
print(f"- {name}\n- {surname}\n- {age}")
'''

''' 
s.split(', ') — делим строку на части
[part.split(': ')[1] for part in ...] — берём только значение после двоеточия для каждой части.
Присваиваем значения переменным name, surname, age.
print с \n выводит каждое значение на новой строке с дефисом.
'''